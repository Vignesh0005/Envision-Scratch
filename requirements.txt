Flask>=3.0.0
Flask-CORS>=4.0.0
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0
Pillow>=10.0.0
scikit-image>=0.21.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0
gunicorn>=21.2.0
psycopg2-binary>=2.9.0
SQLAlchemy>=2.0.0
alembic>=1.12.0
pytest>=7.4.0
black>=23.9.0
flake8>=6.1.0

# WebSocket Camera Service Dependencies
websockets>=11.0

# Camera SDK Dependencies (Optional - require separate installation)
# IDS uEye SDK - requires separate installation
# pyueye>=1.0.0  # Uncomment if available

# Mshot SDK - requires separate installation
# mshot>=1.0.0  # Uncomment if available

# Hikrobot MVS SDK - requires separate installation
# MvCameraControl_class  # Uncomment if available
# Note: Hikrobot SDK is already integrated via MvCameraControl_class.py
